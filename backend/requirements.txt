# GitHub Dev Card Generator — Backend Dependencies
# Install with: uv pip install -r requirements.txt
# Or individually: uv add <package>

# --- Core framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Google ADK + Gemini ---
google-adk>=0.5.0
google-generativeai>=0.8.0

# --- MCP (FastMCP) ---
fastmcp>=2.0.0

# --- HTTP client (used inside MCP tools) ---
httpx>=0.27.0

# --- Data validation ---
pydantic>=2.7.0

# --- Google Cloud (for Cloud Run auth, logging, etc.) ---
google-cloud-logging>=3.10.0
